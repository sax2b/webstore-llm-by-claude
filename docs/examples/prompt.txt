System execution block initiated.

Read and follow the exact step-by-step `Workflow` detailed inside `AGENTS.md` carefully to locate the active pending task, initialize all required context, execute it, and perform the verification build.

Begin execution now. State your target focus clearly before writing code by printing:
"🎯 [TARGET FOCUS] Now executing Task [ID]: [Task Name]"